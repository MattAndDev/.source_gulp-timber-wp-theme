<article class="teaser " id="teaser-{{post.ID}}">

	<h2 class="teaser_title">
		<a href="{{post.link}}">{{post.title}}</a>
	</h2>
	<p>{{post.get_preview}}</p>

	{% if post.get_thumbnail %}
		<img src="{{post.thumbnail.src}}" />
	{% endif %}

</article>
