<div class="comments_responses_item {{comment.comment_type}}" id="comment-{{comment.ID}}">
	<h5 class="comments_responses_item_author">{{comment.author.name}} says</h5>
	<div class="comments_responses_item_tyography">
		{{comment.comment_content|wpautop}}
	</div>
</div>
